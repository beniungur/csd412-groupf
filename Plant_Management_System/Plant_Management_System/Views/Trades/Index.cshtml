@model IEnumerable<Plant_Management_System.Models.Trade>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PlantId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PersonId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ReceivingPlant)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PlantId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PersonId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ReceivingPlant)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TradeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TradeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TradeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
